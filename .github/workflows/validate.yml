name: validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      # Catch drift by testing the minimum plugin contract against the latest release.
      - name: Install the latest published Tamarind CLI
        run: >-
          python -m pip install
          tamarind-cli
          pytest PyYAML
      - name: Run plugin and CLI contract tests
        run: python -m pytest --strict-markers
      - name: Verify installed dependency consistency
        run: python -m pip check
      - name: Compile retained helpers
        run: python -m compileall -q plugins/tamarind/skills
      - name: Run every retained helper from an unrelated directory
        shell: bash
        run: |
          repo="$PWD"
          workdir="$(mktemp -d)"
          cd "$workdir"
          while IFS= read -r script; do
            python "$script" --help >/dev/null
          done < <(find "$repo/plugins/tamarind/skills" -path '*/scripts/*.py' ! -name '_common.py' -type f | sort)
