interface:
  display_name: "Tamarind Batch"
  short_description: "Run one Tamarind tool across many inputs"
  default_prompt: "Use $tamarind-batch to validate and run this tool across many inputs, then inspect and rank completed subjobs."
