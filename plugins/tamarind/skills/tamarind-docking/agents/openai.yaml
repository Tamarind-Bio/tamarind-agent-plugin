interface:
  display_name: "Tamarind Docking"
  short_description: "Dock ligands and rank poses or binding scores"
  default_prompt: "Use $tamarind-docking to choose and validate a docking workflow, then rank the resulting poses with the correct metric direction."
