interface:
  display_name: "Tamarind Fine-tune and Inference"
  short_description: "Fine-tune a Tamarind model and run inference"
  default_prompt: "Use $tamarind-finetune to validate a supported training and inference pair, train it, and compare held-out performance."
