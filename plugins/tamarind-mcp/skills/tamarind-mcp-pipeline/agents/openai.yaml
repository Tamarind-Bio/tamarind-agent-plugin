interface:
  display_name: "Tamarind MCP Pipeline"
  short_description: "Run declarative Tamarind pipelines through MCP"
  default_prompt: "Use $tamarind-mcp-pipeline to author, validate, run and read this Tamarind pipeline."
dependencies:
  tools:
    - type: "mcp"
      value: "tamarind"
      description: "Authenticated Tamarind Bio MCP server"
      transport: "streamable_http"
      url: "https://mcp.tamarind.bio/mcp"
