interface:
  display_name: "Tamarind Custom Tool Deploy"
  short_description: "Deploy your own repo as a Tamarind tool"
  default_prompt: "Use $tamarind-custom-tool-deploy to convert this repository into the Tamarind custom-tool format, build it, and publish it once I approve."
