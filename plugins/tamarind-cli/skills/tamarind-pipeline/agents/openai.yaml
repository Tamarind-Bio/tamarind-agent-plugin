interface:
  display_name: "Tamarind Pipeline"
  short_description: "Chain Tamarind jobs as resumable CLI stages"
  default_prompt: "Use $tamarind-pipeline to turn this multi-tool workflow into resumable validated stages with explicit artifact handoffs."
